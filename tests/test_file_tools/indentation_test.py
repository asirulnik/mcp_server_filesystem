def example_function():
    # This is indented with 4 spaces
    if new_condition:
        # Modified comment
        print("Changed text")
        for item in items:
            # This is indented with 12 spaces
            process(item)
